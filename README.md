# Assessment 2

Centralized repository for the Assessment 2 project with separate frontend and backend components.

## Project Structure
- `frontend/` – client-side application.
- `backend/` – server-side services and APIs.

## Usage
1. Clone the repository.
2. Work inside the `frontend` or `backend` folders as appropriate.
3. Keep this root README as the single source of documentation.
